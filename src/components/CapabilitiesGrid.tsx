import React from 'react';
import { Cpu, EyeOff, Fingerprint, Layers, Lock, ShieldCheck } from 'lucide-react';
const caps=[['Local witness execution','Sensitive inputs remain in browser memory.',Lock],['In-browser proof generation','Compact circuits generate proofs on the client.',Cpu],['Selective disclosure','Only explicit public state crosses the privacy boundary.',Layers],['Zero plaintext exposure','Validators verify outcomes without seeing payroll data.',EyeOff],['Verifiable consensus','Proofs settle against Midnight network state.',Fingerprint],['Audit-ready records','Every accepted transition produces durable evidence.',ShieldCheck]] as const;
export const CapabilitiesGrid:React.FC=()=> <section id="capabilities" className="py-20"><div className="mb-10 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-xs uppercase tracking-[.18em] text-[#a995ff]">Platform capabilities</p><h2 className="mt-3 text-3xl font-medium tracking-[-.035em] sm:text-4xl">A privacy stack finance can operate.</h2></div><p className="max-w-md text-sm leading-6 text-[var(--text-muted)]">From confidential inputs to a verifiable ledger commitment, every layer has a clear responsibility.</p></div><div className="grid overflow-hidden rounded-2xl border border-[var(--border)] md:grid-cols-2 lg:grid-cols-3">{caps.map(([title,copy,Icon],i)=><article key={title} className="border-b border-r border-[var(--border)] bg-[var(--surface-1)] p-6 last:border-b-0"><Icon className="h-5 w-5 text-[#a995ff]"/><h3 className="mt-5 font-medium">{title}</h3><p className="mt-2 text-sm leading-6 text-[var(--text-muted)]">{copy}</p><span className="mt-6 block font-mono text-[10px] text-[var(--text-subtle)]">0{i+1} / VANSIDIAN</span></article>)}</div></section>;
