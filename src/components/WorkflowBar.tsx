import React from 'react';
import { Check, KeyRound, ShieldCheck, Wallet } from 'lucide-react';
interface Props { isConnected:boolean; hasWitnessValue:boolean; isConfirmed:boolean; }
export const WorkflowBar:React.FC<Props>=({isConnected,hasWitnessValue,isConfirmed})=>{const steps=[['Connect','Authorize your Lace wallet',Wallet,isConnected],['Prepare','Review private payroll inputs',KeyRound,isConnected&&hasWitnessValue],['Prove','Commit verified state',ShieldCheck,isConfirmed]] as const; return <div className="overflow-hidden rounded-2xl border border-[var(--border)] bg-[var(--surface-1)]"><div className="border-b border-[var(--border)] px-6 py-5"><p className="text-xs uppercase tracking-[.18em] text-[#a995ff]">How it works</p><h2 className="mt-2 text-2xl font-medium">One clear path from payroll to proof.</h2></div><div className="grid md:grid-cols-3">{steps.map(([title,copy,Icon,done],i)=><div key={title} className="relative border-b border-[var(--border)] p-6 last:border-0 md:border-b-0 md:border-r"><div className="flex items-center justify-between"><span className={`grid h-10 w-10 place-items-center rounded-full ${done?'bg-[#32b77c]/12 text-[#73d7aa]':'bg-white/[.04] text-[var(--text-muted)]'}`}>{done?<Check className="h-4 w-4"/>:<Icon className="h-4 w-4"/>}</span><span className="font-mono text-xs text-[var(--text-subtle)]">0{i+1}</span></div><h3 className="mt-5 font-medium">{title}</h3><p className="mt-1 text-sm text-[var(--text-muted)]">{copy}</p></div>)}</div></div>};
